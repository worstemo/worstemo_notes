### 酒店管理系统项目分工表

| 项目名称       | 成员       | 负责开发模块 (开发任务)                                                                                                                                                                                                                  | 负责撰写文档 (文档任务)                          |
| ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------- |
| **酒店管理系统** | **成员 W** | **1. 后端：客房管理模块 (rooms/)** • 开发 `Room` 数据模型 (`models.py`) • 开发客房后台管理界面 (`admin.py`)，集成图片上传与预览功能 **2. 前端**：设计与实现自定义登录页面 (`templates/custom_login.html`)                                                                          | 1. **项目前景与范围文档.docx** 2. **用户手册.docx** |
| **酒店管理系统** | **成员 X** | **1. 后端：预订管理模块 (reservations/)** • 开发 `Reservation` 数据模型 (`models.py`)，实现客户与房间的外键关联 • 开发预订后台管理界面 (`admin.py`)，管理完整预订流程 **2. 后端：客户管理模块 (customers/)** • 开发 `Customer` 数据模型 (`models.py`)，设计会员与积分体系 • 开发客户后台管理界面 (`admin.py`)    | 1. **用例文档.docx** 2. **软件测试报告.docx**    |
| **酒店管理系统** | **成员 Y** | **1. 后端：财务管理模块 (finance/)** • 开发 `Income` (收入) 与 `Expense` (支出) 数据模型 (`models.py`) • 开发财务后台管理双界面 (`admin.py`)，实现收支分类管理 **2. 项目全局配置与系统测试** • 配置项目全局设置 (`settings.py`)，集成SimpleUI美化 • 配置项目主路由 (`urls.py`)，实现后台统一入口               | 1. **UML设计文档.docx** 2. **需求分析文档.docx** |
| **酒店管理系统** | **成员 Z** | **1. 员工管理模块 (employees/)** • 开发 `Employee` 数据模型 (`models.py`) • 开发员工后台管理界面 (`admin.py`) **2. 数据库设计与核心模型** • **负责全部五个模块的数据模型(`models.py`)设计、关联与优化** • 编写数据库初始化脚本 (`hotel_management_db.sql`) • 负责数据库迁移文件 (`migrations/`) 的生成与管理 | 1. **数据库设计文档.docx** 2. **用户手册.docx**   |
